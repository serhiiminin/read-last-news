import variables from '../../styles/variables';

const styles = {
  root: {
    background: variables.colors.messages,
    color: variables.colors.text,
    borderRadius: '5px',
    fontWeight: 'bold',
    opacity: '.6',
    alignSelf: 'center',
  },
};

export default styles;
