import { variables } from '../../styles';

const styles = {
  'titles-empty': {
    fontStyle: 'italic',
    opacity: '.5',
  },
  'titles-list': {
    margin: variables.margins.no,
    padding: '0',
  },
};

export default styles;
