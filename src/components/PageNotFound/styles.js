const styles = {
  wrapper: {
    display: 'grid',
    justifyContent: 'center',
    alignItems: 'center',
    minHeight: '100vh',
    width: '100%',
  },
  buttonWrapper: {
    textAlign: 'center',
  },
};

export default styles;
